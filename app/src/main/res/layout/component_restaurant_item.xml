<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/restaurantImage"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_gravity="center_vertical"
        android:importantForAccessibility="no"
        tools:src="@tools:sample/backgrounds/scenic" />

    <TextView
        android:id="@+id/restaurantName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginStart="8dp"
        android:textAppearance="@style/TextAppearance.Body1"
        tools:text="Knabberecke" />

    <!-- TODO: Verschönere das Layout der Listenelemente:
    - jedes Element soll eine feste Höhe von 64dp erhalten
    - jedes Element soll eine Material Design "Card" sein (MaterialCardView)
        - die Card soll 4dp Höhe besitzen
    - innerhalb der Card sollen sich das Bild und der Restaurantname befinden
    - das Bild soll quadratisch sein und allen verfügbaren vertikalen Platz beanspruchen
    - der Text soll rechts vom Bild erscheinen (8dp Abstand) und in der Card vertikal zentriert sein
    - ConstraintLayout ist dein Freund
    -->
</LinearLayout>
